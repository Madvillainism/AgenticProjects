<header class="header">
  <nav class="header__nav">
    <a href="/" class="header__logo">A M A T E R A S U</a>
    <div class="header__actions">
      <button class="search-toggle" id="searchToggle" aria-label="Search">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      </button>
      <span class="header__tag">GRAFFITI</span>
      <button class="header__cart" id="cartToggle" aria-label="Open cart">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/></svg>
        <span class="cart-badge" id="cartCount">0</span>
      </button>
    </div>
  </nav>
</header>

<style>
  .header { position: fixed; top: 0; left: 0; right: 0; z-index: 50; background: var(--glass-bg); backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px); border-bottom: 1px solid var(--glass-border); }
  .header__nav { display: flex; align-items: center; justify-content: space-between; max-width: var(--max-width); margin: 0 auto; padding: 0.75rem 1rem; }
  .header__logo { font-family: var(--font-display); font-size: clamp(1.2rem, 4vw, 1.8rem); letter-spacing: 0.25em; color: var(--text-primary); transition: color 0.2s; }
  .header__logo:active { color: var(--neon-cyan); }
  .header__actions { display: flex; align-items: center; gap: 0.5rem; }
  .search-toggle { padding: 0.5rem; color: var(--text-secondary); transition: color 0.2s; display: flex; }
  .search-toggle:active { color: var(--neon-cyan); }
  .header__tag {
    font-family: var(--font-display); font-size: 0.6rem; letter-spacing: 0.2em;
    padding: 0.2rem 0.5rem; border: 1px solid var(--neon-magenta);
    color: var(--neon-magenta); border-radius: 4px; opacity: 0.7;
    display: none;
  }
  @media (min-width: 600px) { .header__tag { display: inline; } }
  .header__cart { position: relative; padding: 0.5rem; color: var(--text-primary); transition: color 0.2s; display: flex; align-items: center; }
  .header__cart:active { color: var(--neon-cyan); }
</style>